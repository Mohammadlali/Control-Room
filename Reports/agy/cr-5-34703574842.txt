بر اساس مستندات رسمی سازمان (`README.md`، `Team/START_HERE.md` و `Team/COMPANY_SCOPE.md`):

---

### ۱. کنترل-روم (Control-Room) روی کدام حساب است؟
مخزن **Control-Room** دقیقاً روی حساب **`ACC0`** با نام کاربری **`Mohammadlali`** (شناسه/Nickname: `1997`) میزبانی می‌شود.
* این حساب منحصراً مسئول مدیریت، حاکمیت و دیسپچ سازمان است و کد هیچ محصول یا پروژه دیگری روی آن توسعه داده نمی‌شود.
* (نسخه دوم و قدیمی روی حساب `ACC6` / `07` کاملاً بازنشسته و حذف شده است).

---

### ۲. چند پروژه فعال زیر نظر آن است؟
در کل سازمان (شامل ۹ حساب `ACC0` تا `ACC8`)، وضعیت پروژه‌ها بر اساس جدول هدایت موضوعی (`Routing Table`) به شرح زیر است:

#### الف) پروژه‌های محصولی مستقل (۲ پروژه فعال):
1. **[TEHRAN: BLIND SPOT (TBS)](file:///Team/COMPANY_SCOPE.md#L37)**:
   * **میزبان اختصاصی:** حساب **`ACC6`** (`mohammadlali0707-stack` / `07`) روی مخزن `Claud-Cloud-Project`.
2. **[AirboxVIP Coffeenet](file:///Team/COMPANY_SCOPE.md#L32)**:
   * **میزبان اختصاصی:** حساب **`ACC1`** (`momonakikugava-pixel` / `Momona`) روی مخزن `AirboxVIP_Coffeenet`.

#### ب) ابزارهای حاکمیتی و مانیتورینگ سازمان (روی `ACC0`):
* **[status-dashboard](file:///Team/COMPANY_SCOPE.md#L31)**: داشبورد پایش وضعیت کل سازمان (`status.airboxvip.top`) که از تاریخ ۲۰۲۶-۰۹-۱۲ به عنوان یک پروژه قابل مسیریابی به `ACC0` اضافه شده است.
* **[Control-Room](file:///Team/COMPANY_SCOPE.md#L31)**: خود اتاق فرمان و سیستم حاکمیت مرکزی.

#### وضعیت سایر حساب‌ها:
حساب‌های **`ACC2`، `ACC3`، `ACC4`، `ACC5`، `ACC7` و `ACC8`** در حال حاضر پروژه مستقلی از خود ندارند و صرفاً به عنوان ورکر محاسباتی در رینگ دامنه‌های TBS (`TBS ring domain`) فعالیت می‌کنند.

---

> **خلاصه عددی:**
> * **۲ پروژه محصولی خارجی:** TBS (روی ACC6) و AirboxVIP (روی ACC1).
> * **۱ پروژه ابزار داخلی تحت مدیریت:** status-dashboard (روی ACC0).
