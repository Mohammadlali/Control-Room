# وضعیت همین لحظه — Control-Room

> **نکته:** این فایل را در شروع هر نشست تازه به عنوان وضعیت زنده مطالعه کنید. نیازی به بررسی کل فایل‌های ریپازیتوری نیست.

---

## ۱. وضعیت جاری
- **وضعیت هسته:** ریپازیتوری `Control-Room` روی حساب `07` (`mohammadlali0707-stack`) با معماری استاندارد دکترین اتاق فرمان ایجاد شد.
- **بات رسمی گیت‌هاب:** ورک‌فلو `@gemini-cli` با استخر فال‌بک ۸ کلیده فعال و متصل شد.


---

## ۲. پروژه‌های تحت نظر در این اتاق فرمان
1. **`Control-Room`** (همین ریپازیتوری): مدیریت تسک‌ها، کنترل و هماهنگی.
2. **`agw-workers`**: رانرها و کارهای ابری GitHub Actions.
3. **پروژه‌های در صف ادغام:**
   - `AirboxVIP_Coffeenet` (ربات تلگرام و سرویس‌های مرتبط)


---

## ۳. قدم قطعی بعدی (Next Step)
- اجرای دروازه اعتبارسنجی اولیه:
  ```bash
  python Tools/run_gates.py
  ```
- ثبت اولین کامیت زیرساخت در `Team/CHANGELOG.md` و پوش به `origin main`.
- بررسی و تعریف اولین تسک پروژه در `Team/tasks/pending/`.
